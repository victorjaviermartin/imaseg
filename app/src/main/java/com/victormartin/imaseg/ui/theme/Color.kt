package com.victormartin.imaseg.ui.theme

import androidx.compose.ui.graphics.Color

// Define custom background colors
val ImasegBeigeWhite = Color(0xFFFFF8E1) // Blanco beige
val ImasegSlateBlack = Color(0xFF1E1E1E) // Negro pizarra

// Define custom background colors
val DarkGreen = Color(0xFF0C3A2D) // Verde oscuro, para el fondo del tema oscuro y el texto principal
val LightGreen = Color(0xFF6B9773) // Verde claro, para el texto y componentes en el tema oscuro
val Yellow = Color(0xFFFFBB02) // Amarillo, para advertencias
val LightBackground = Color(0xFFECFBEF) // Fondo claro
val Orange = Color(0xFFB36618) // Naranja, para detalles y estados
val OffWhite = Color(0xFFFBFBF9) // Fondo claro, usado en el tema claro

// Colores para los estados (success, error, warning)
val SuccessColor = Color(0xFF4f772d) // Verde para el estado de éxito
val ErrorColor = Color(0xFFbc4b51) // Rojo para el estado de error
val WarningColor = Color(0xFFffc857) // Amarillo para el estado de advertencia

// Contraste de colores
val DarkGreenContrast = Color(0xFFFFFFFF) // Contraste blanco para DarkGreen
val LightGreenContrast = Color(0xFF0C3A2D) // Contraste oscuro para LightGreen
val YellowContrast = Color(0xFF0C3A2D) // Contraste oscuro para Yellow
val LightBackgroundContrast = Color(0xFF0C3A2D) // Contraste oscuro para LightBackground
val OrangeContrast = Color(0xFFFFFFFF) // Contraste blanco para Orange
val OffWhiteContrast = Color(0xFF0C3A2D) // Contraste oscuro para OffWhite
